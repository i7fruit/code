#include <stdio.h>
#include <limits.h>

typedef strut node
{
    int val;
    struct node *next;
}
node;

int main(void)
{
    node 

    int i = 0;

    do
    {
        word[i] = getchar();
    }
    while (word[i++] != '\n');

    printf("%s\n", word);

    return 0;
}